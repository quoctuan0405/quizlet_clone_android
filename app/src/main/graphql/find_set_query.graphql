query findSet($query: String) {
    findSet(query: $query) {
        id
        name
        _count {
            terms
        }
        author {
            id
            username
        }
    }
}